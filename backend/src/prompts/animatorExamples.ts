export const ANIMATOR_FEW_SHOT_EXAMPLES = `EXAMPLE (BANNER – ScenePlan → MotionScript)\n\nimport { Stage, Motion } from '@motiongen/sdk';\n\nconst width = 800;\nconst height = 600;\nconst duration = 5;\nconst fps = 30;\n\ntype LayoutPoint = [number, number];\n\ninterface GridLayoutOptions {\n  margin?: number;\n  columns?: number;\n}\n\nfunction placeBanner(\n  region: 'top' | 'center' | 'bottom',\n  width: number,\n  height: number\n): LayoutPoint {\n  const factor = region === 'top' ? 0.25 : region === 'bottom' ? 0.75 : 0.5;\n  return [width / 2, height * factor];\n}\n\nfunction placeColumn(\n  side: 'left' | 'right',\n  rowIndex: number,\n  totalRows: number,\n  width: number,\n  height: number\n): LayoutPoint {\n  const marginX = width * 0.08;\n  const top = height * 0.25;\n  const bottom = height * 0.9;\n  const availableHeight = bottom - top;\n  const rowHeight = availableHeight / Math.max(totalRows, 1);\n  const centerX =\n    side === 'left'\n      ? marginX + width * 0.25\n      : width - marginX - width * 0.25;\n  const centerY = top + rowHeight * (rowIndex + 0.5);\n  return [centerX, centerY];\n}\n\nfunction placeInGrid(\n  index: number,\n  total: number,\n  width: number,\n  height: number,\n  options: GridLayoutOptions = {}\n): LayoutPoint {\n  const margin = options.margin !== undefined ? options.margin : 40;\n  const columns =\n    options.columns && options.columns > 0\n      ? options.columns\n      : Math.ceil(Math.sqrt(total));\n  const rows = Math.ceil(total / columns);\n  const usableWidth = width - margin * 2;\n  const usableHeight = height - margin * 2;\n  const cellWidth = usableWidth / columns;\n  const cellHeight = usableHeight / rows;\n  const col = index % columns;\n  const row = Math.floor(index / columns);\n  const x = margin + cellWidth * (col + 0.5);\n  const y = margin + cellHeight * (row + 0.5);\n  return [x, y];\n}\n\nconst bannerStage = Stage.create(width, height, duration, fps);\n\n// Background (subtle motion)\nconst bg = bannerStage.addShape('rectangle', {\n  width,\n  height,\n  fillColor: [0.04, 0.09, 0.16]\n});\nbg.animate({\n  props: {\n    opacity: { from: 0, to: 100 },\n    position: { from: [width / 2, height / 2 + 10], to: [width / 2, height / 2] }\n  },\n  delay: 0\n});\n\n// Main text headline\nconst mainText = bannerStage.addText('Main headline', {\n  fontSize: 56,\n  fontFamily: 'Arial',\n  color: [1, 1, 1],\n  justification: 2\n});\nconst [mainX, mainY] = placeBanner('top', width, height);\nmainText.animate({\n  props: {\n    position: { from: [mainX, height + 80], to: [mainX, mainY] },\n    opacity: { from: 0, to: 100 }\n  },\n  spring: Motion.spring({ stiffness: 220, damping: 22 }),\n  delay: 0.2\n});\n\n// Supporting lines (staggered foreground text)\nconst lines = ['Supporting line 1', 'Supporting line 2', 'Supporting line 3'];\nconst textElements = lines.map((text, index) => {\n  const side = index % 2 === 0 ? 'left' : 'right';\n  const rowIndex = Math.floor(index / 2);\n  const totalRows = Math.ceil(lines.length / 2);\n  const [x, y] = placeColumn(side, rowIndex, totalRows, width, height);\n  const el = bannerStage.addText(text, {\n    fontSize: 28,\n    fontFamily: 'Arial',\n    color: [0.8, 0.9, 1],\n    justification: 2\n  });\n\n  el.animate({\n    props: {\n      position: {\n        from: [x, y + 40],\n        to: [x, y]\n      },\n      opacity: { from: 0, to: 100 }\n    },\n    delay: 0\n  });\n\n  return el;\n});\n\nconst bannerGroup = bannerStage.createGroup();\nbannerGroup.stagger(\n  textElements,\n  {\n    props: {\n      opacity: { from: 0, to: 100 }\n    },\n    delay: 0\n  },\n  { delay: 0.18 }\n);\n\n// Accent circle (midground)\nconst accent = bannerStage.addShape('circle', {\n  radius: 40,\n  fillColor: [0.2, 0.7, 1]\n});\nconst [accentX, accentY] = placeInGrid(0, 1, width, height, { margin: 80 });\naccent.animate({\n  props: {\n    position: { from: [accentX, accentY + 80], to: [accentX, accentY] },\n    scale: { from: [60, 60], to: [100, 100] }\n  },\n  spring: Motion.spring({ stiffness: 180, damping: 18 }),\n  delay: 0.6\n});\n\nexport default bannerStage.toJSON();\n\n\nEXAMPLE (GAME-DEMO – ScenePlan → MotionScript)\n\nimport { Stage as GameStage, Motion as GameMotion } from '@motiongen/sdk';\n\nconst gameWidth = 800;\nconst gameHeight = 600;\nconst gameDuration = 5;\nconst gameFps = 30;\n\nconst gameStage = GameStage.create(gameWidth, gameHeight, gameDuration, gameFps);\n\n// Playfield (table/court)\nconst table = gameStage.addShape('rectangle', {\n  width: gameWidth * 0.8,\n  height: gameHeight * 0.4,\n  fillColor: [0.02, 0.15, 0.27]\n});\ntable.animate({\n  props: {\n    opacity: { from: 0, to: 100 }\n  },\n  delay: 0\n});\n\n// Left and right paddles\nconst paddleLeft = gameStage.addShape('rectangle', {\n  width: 18,\n  height: 90,\n  fillColor: [0.9, 0.9, 0.9]\n});\nconst paddleRight = gameStage.addShape('rectangle', {\n  width: 18,\n  height: 90,\n  fillColor: [0.9, 0.9, 0.9]\n});\n\n// Ball\nconst ball = gameStage.addShape('circle', {\n  radius: 10,\n  fillColor: [1, 1, 1]\n});\n\n// Scoreboard text\nconst score = gameStage.addText('0 : 0', {\n  fontSize: 32,\n  fontFamily: 'Arial',\n  color: [1, 1, 1]\n});\n\n// Animate paddles into place\nconst paddleOffsetY = gameHeight * 0.15;\nconst paddleXMargin = gameWidth * 0.1;\n\npaddleLeft.animate({\n  props: {\n    position: {\n      from: [paddleXMargin, gameHeight / 2 + 200],\n      to: [paddleXMargin, gameHeight / 2 - paddleOffsetY]\n    },\n    opacity: { from: 0, to: 100 }\n  },\n  spring: GameMotion.spring({ stiffness: 220, damping: 20 }),\n  delay: 0.2\n});\n\npaddleRight.animate({\n  props: {\n    position: {\n      from: [gameWidth - paddleXMargin, gameHeight / 2 - 200],\n      to: [gameWidth - paddleXMargin, gameHeight / 2 + paddleOffsetY]\n    },\n    opacity: { from: 0, to: 100 }\n  },\n  spring: GameMotion.spring({ stiffness: 220, damping: 20 }),\n  delay: 0.25\n});\n\n// Ball rally arc across the table\nball.animate({\n  props: {\n    position: {\n      from: [gameWidth / 2, gameHeight / 2 - 80],\n      to: [gameWidth / 2 + 140, gameHeight / 2 + 40]\n    }\n  },\n  spring: GameMotion.spring({ stiffness: 260, damping: 24 }),\n  delay: 0.4\n});\n\nball.animate({\n  props: {\n    position: {\n      from: [gameWidth / 2 + 140, gameHeight / 2 + 40],\n      to: [gameWidth / 2 - 140, gameHeight / 2 - 40]\n    }\n  },\n  spring: GameMotion.spring({ stiffness: 260, damping: 24 }),\n  delay: 1.1\n});\n\n// Score update emphasis\nscore.animate({\n  props: {\n    position: {\n      from: [gameWidth / 2, 40],\n      to: [gameWidth / 2, 40]\n    },\n    scale: { from: [90, 90], to: [110, 110] },\n    opacity: { from: 0, to: 100 }\n  },\n  spring: GameMotion.spring({ stiffness: 200, damping: 20 }),\n  delay: 1.6\n});\n\nexport default gameStage.toJSON();\n\n\nEXAMPLE (PRODUCT-DEMO – ScenePlan → MotionScript)\n\nimport { Stage as UiStage, Motion as UiMotion } from '@motiongen/sdk';\n\nconst uiWidth = 1080;\nconst uiHeight = 1920;\nconst uiDuration = 6;\nconst uiFps = 30;\n\nconst uiStage = UiStage.create(uiWidth, uiHeight, uiDuration, uiFps);\n\n// Background panel\nconst bgPanel = uiStage.addShape('rectangle', {\n  width: uiWidth,\n  height: uiHeight,\n  fillColor: [0.03, 0.05, 0.08]\n});\nbgPanel.animate({\n  props: {\n    opacity: { from: 0, to: 100 }\n  },\n  delay: 0\n});\n\n// Device frame (rounded rectangle)\nconst deviceFrame = uiStage.addShape('roundedRectangle', {\n  width: uiWidth * 0.8,\n  height: uiHeight * 0.7,\n  cornerRadius: 40,\n  fillColor: [0.08, 0.12, 0.18]\n});\ndeviceFrame.animate({\n  props: {\n    position: {\n      from: [uiWidth / 2, uiHeight + 400],\n      to: [uiWidth / 2, uiHeight / 2]\n    },\n    opacity: { from: 0, to: 100 }\n  },\n  spring: UiMotion.spring({ stiffness: 220, damping: 22 }),\n  delay: 0.2\n});\n\n// Header text\nconst header = uiStage.addText('Product Dashboard', {\n  fontSize: 60,\n  fontFamily: 'Arial',\n  color: [1, 1, 1],\n  justification: 2\n});\nheader.animate({\n  props: {\n    position: {\n      from: [uiWidth / 2, uiHeight / 2 - 260],\n      to: [uiWidth / 2, uiHeight / 2 - 260]\n    },\n    opacity: { from: 0, to: 100 }\n  },\n  delay: 0.4\n});\n\n// Primary UI card\nconst card = uiStage.addShape('roundedRectangle', {\n  width: uiWidth * 0.65,\n  height: uiHeight * 0.18,\n  cornerRadius: 24,\n  fillColor: [0.12, 0.18, 0.32]\n});\ncard.animate({\n  props: {\n    position: {\n      from: [uiWidth / 2, uiHeight / 2 - 40],\n      to: [uiWidth / 2, uiHeight / 2 - 40]\n    },\n    opacity: { from: 0, to: 100 }\n  },\n  delay: 0.5\n});\n\n// Call-to-action button\nconst ctaButton = uiStage.addShape('roundedRectangle', {\n  width: uiWidth * 0.4,\n  height: 90,\n  cornerRadius: 30,\n  fillColor: [0.2, 0.65, 0.98]\n});\nctaButton.animate({\n  props: {\n    position: {\n      from: [uiWidth / 2, uiHeight / 2 + 220],\n      to: [uiWidth / 2, uiHeight / 2 + 220]\n    },\n    scale: { from: [90, 90], to: [100, 100] },\n    opacity: { from: 0, to: 100 }\n  },\n  spring: UiMotion.spring({ stiffness: 230, damping: 24 }),\n  delay: 0.8\n});\n\n// Pointer/highlight element moving across the UI\nconst pointer = uiStage.addShape('circle', {\n  radius: 18,\n  fillColor: [1, 1, 1]\n});\npointer.animate({\n  props: {\n    position: {\n      from: [uiWidth / 2 - 150, uiHeight / 2 - 40],\n      to: [uiWidth / 2 + 150, uiHeight / 2 - 40]\n    }\n  },\n  easing: 'easeInOut',\n  delay: 1.2,\n  duration: 1.0\n});\n\npointer.animate({\n  props: {\n    position: {\n      from: [uiWidth / 2 + 150, uiHeight / 2 - 40],\n      to: [uiWidth / 2, uiHeight / 2 + 220]\n    }\n  },\n  easing: 'easeInOut',\n  delay: 2.4,\n  duration: 0.8\n});\n\nexport default uiStage.toJSON();\n`;
